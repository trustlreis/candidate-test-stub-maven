name: ChatGPT

on:
  pull_request:
    types: [opened, synchronize]

jobs:

  code_review_job:
    runs-on: ubuntu-latest
    name: ChatGPT Code Review

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 # needed to checkout all branches for the diff action to work

      # Check the PR diff using the current branch and the base branch of the PR
      - uses: GrantBirki/git-diff-action@v2.4.1
        id: git-diff-action
        with:
          json_diff_file_output: diff.json
          raw_diff_file_output: diff.txt
          file_output_only: "false"

      - uses: koganei/llm-code-review@main
        name: "Code Review"
        id: review
        env:
          DIFF: ${{ steps.git-diff-action.outputs.raw-diff-path }}
        with:
          apiKey: ${{ secrets.OPENAI_TOKEN }}
          githubToken: ${{ secrets.CUSTOM_GITHUB_TOKEN }}
          githubRepository: ${{ github.repository }}
          githubPullRequestNumber: ${{ github.event.pull_request.number }}
          gitCommitHash: ${{ github.event.pull_request.head.sha }}
          temperature: "0.2"
          maxNewTokens: "250"
          topK: "50"
          topP: "0.95"
          pullRequestDiff: |-
            $(cat $DIFF)
          pullRequestChunkSize: "3500"
          logLevel: "DEBUG"

    #   - name: Checkout Repo
    #     uses: actions/checkout@v3

    #   - name: AI Code Reviewer
    #     uses: hohyon-ryu/ai-codereviewer@code_review
    #     with:
    #       GITHUB_TOKEN: ${{ secrets.CUSTOM_GITHUB_TOKEN }} # The GITHUB_TOKEN is there by default so you just need to keep it like it is and not necessarily need to add it as secret as it will throw an error. [More Details](https://docs.github.com/en/actions/security-guides/automatic-token-authentication#about-the-github_token-secret)
    #       OPENAI_API_KEY: ${{ secrets.OPENAI_TOKEN }}
    #       OPENAI_API_MODEL: "gpt-4o" # Optional: defaults to "gpt-4"
    #       exclude: "**/*.json, **/*.md" # Optional: exclude patterns separated by commas. defaults to  "**/*.json, **/*.md, yarn.lock, package-lock.json"
    #       language: "english" # Optional: defaults to "english"

          
    # steps:
    #     - name: Checkout Repor
    #       uses: actions/checkout@v3

    #     - name: OpenAI ChatGPT Code Review
    #       uses: manjunathsb100/chatgpt-code-review-action@1.0.7
    #       with:
    #         PROGRAMMING_LANGUAGE: 'JavaScript'
    #         OPENAI_TOKEN: ${{ secrets.OPENAI_TOKEN }}
    #         GITHUB_TOKEN: ${{ secrets.CUSTOM_GITHUB_TOKEN }}
    #         FULL_REVIEW_COMMENT: 'chatgpt'
    #         REVIEW_COMMENT_PREFIX: 'chatgpt:'